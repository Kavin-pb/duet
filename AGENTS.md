# AGENTS.md

## Agent skills

### Issue tracker

Issues, features, and specs are tracked in Linear (workspace "chat chat", team "Chat chat") via the Linear MCP tools. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary — `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix` — applied as Linear issue labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
